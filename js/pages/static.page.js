/**
 * AstroNuit — Pages statiques (mentions-legales, charte)
 * Navbar + auth state + SW uniquement.
 */

import { initAuthNavbar, registerServiceWorker } from '../utils/app.js';

initAuthNavbar();
registerServiceWorker();
